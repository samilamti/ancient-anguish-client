name: ancient_anguish_server
description: WebSocket-to-TCP proxy and per-user file storage server for the Ancient Anguish web client.
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.1
  web_socket_channel: ^3.0.2
  dart_jsonwebtoken: ^2.14.4
  dbcrypt: ^2.0.0
  args: ^2.6.0
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.15
  http: ^1.3.0
